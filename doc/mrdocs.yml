# Input
source-root: ..
# Directories that contain documented source files
input:
  - ../include
# Patterns to filter out the source-files in the directories
file-patterns:
  - '*.hpp'

# Filters
include-symbols:
  - 'boost::runtime_services::**'
implementation-defined:
  - 'boost::runtime_services::detail'
  - 'boost::runtime_services::*::detail'
inaccessible-members: never
inaccessible-bases: never

# Generator
generate: adoc
base-url: https://www.github.com/cppalliance/runtime_services/blob/develop/include/

# Style
verbose: true
multipage: true
use-system-libc: true

cmake: '-DCMAKE_CXX_STANDARD=20 -DBOOST_RUNTIME_SERVICES_BUILD_TESTS=OFF -DBOOST_RUNTIME_SERVICES_BUILD_EXAMPLES=OFF'
